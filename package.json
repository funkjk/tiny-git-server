{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  },
  "scripts": {
    "build":"lerna run build",
    "dev":"cd examples/node-examples && nodemon"
  }
}
